# План: редизайн сторінок курсу за продажним лендингом

Дата: 17.08.2026.

Референс: `k:\Downloads\IPRM-plazmogel-landing-ready\IPRM-plazmogel-selling-landing.html`
(480 рядків, самодостатній HTML із власними токенами) + 9 зображень у
`plazmogel-landing-assets/`.

## Статус виконання

| Трек | Стан |
|---|---|
| A -- дані: моделі та міграції | **зроблено** (не закомічено) |
| B -- адмінка | **зроблено** (не закомічено) |
| C -- CSS-компоненти | **зроблено** (не закомічено) |
| D -- шаблон очного курсу | **зроблено** (не закомічено) |
| E -- шаблон онлайн-курсу | **зроблено** (не закомічено) |
| F -- JS-інтерактив | **зроблено** (не закомічено) |
| G -- лід-форма і заявки | **зроблено** (не закомічено) |
| H -- i18n, SEO, тести | **зроблено** (не закомічено) |

Міграції на прод НЕ застосовані. Черга (після наявної `online_cover_20260817`):
`course_landing_20260817`, `program_blocks_poly_20260817`, далі
`thankyou_online_20260817`. Останню перечеплено в кінець лінії: спершу вона
виходила з `promo_online_20260817`, паралельно з нею звідти ж пішла лінія
лендінгу, і деплой упав на `Multiple head revisions are present`. Уся черга
перевірена офлайновим рендером DDL в обидва боки
(`flask db upgrade/downgrade --sql`), до спільної з продом БД не
підключались. Тестів у наборі: 1732, усі проходять.

Лишилось операційне, не код: залити 6 фото «Плазмогелю» в медіа-реєстр
(трек B, п. 6) і заповнити контент решти курсів в адмінці. Заливка пише
одразу в прод -- робити після деплою міграцій і за явною командою.

Зроблене у треку A:

* `Course` -- `proof_stats`, `benefits`, `practice_note_title`,
  `practice_note_text`, `gallery_intro` (усі в `__translatable__`) + властивість
  `gallery`;
* `OnlineCourse` -- ті самі п'ять + `target_audience`, `faq`, `final_cta_text`,
  `trainer_id`, зв'язки `trainer` і `program_blocks`, властивість `gallery`;
* `ProgramBlock` -- поліморфний (`course_id` nullable + `online_course_id`),
  `CheckConstraint ck_program_blocks_single_owner`, властивість `owner`;
* `Review.online_course_id` -- без CHECK: відгук без прив'язки штатний;
* `Trainer.highlights`; `MediaFile.caption`;
* `InstanceTariff` -- `badge` (перекладне), `is_featured` + властивість
  `description_entries` (конвенція «+» для переваг тарифу; наявне
  `description_items` не чіпали -- ним користуються адмінка й форма реєстрації);
* `CourseRequest` -- `name`, `messenger` (+CHECK, +`MESSENGERS`,
  +`messenger_label`), `consent_at`;
* міграції `course_landing_20260817` і `program_blocks_poly_20260817`;
* `docs/models.md` оновлено (Course, CourseRequest, Trainer, ProgramBlock,
  OnlineCourse, Constraints).

Зроблене у треку C:

* новий `app/static/css/course-landing.css` -- один власник компонентів для
  обох сторінок курсу;
* **знахідка:** два потрібні компоненти вже існували, але нікуди не були
  підключені -- `.iprm-anchor-nav` (мертвий CSS у `page-courses.css`) і
  `.iprm-trust-stats` разом із партіалом `partials/_event_trust.html`, який
  ніде не інклюдиться і містить захардкоджені цифри. Перше стало
  `.iprm-page-nav` (+ активний стан, + індикатор місць), друге --
  `.iprm-trust-stats--band`;
* нові компоненти: `iprm-hero--landing` (чипи, KPI, фото, картка місць,
  `iprm-progress`), `iprm-benefit-grid/card`, `iprm-audience-grid/card`,
  `iprm-check-list`, `iprm-gallery`, `iprm-practice-note`,
  `iprm-trainer-block--rich` (теги + цифри), `iprm-dark-shell` +
  `iprm-review-tile`, `iprm-eyebrow`, `iprm-lead-section` + `iprm-lead-form`,
  `iprm-sticky-cta__actions/--ghost`;
* перенесено з `page-courses.css` (там вони мали одного споживача, а тепер
  потрібні двом): FAQ, блок тренера, sticky-CTA, trust-stats, anchor-nav.
  `page-courses.css` схуд із 2055 до 1731 рядка, і каталог курсів та головна
  більше не тягнуть CSS, який ніколи не використовували;
* FAQ переверстано за референсом: тонкі роздільники, «+» повертається на
  45deg замість підміни символу на «-» (плавно й не залежить від шрифту);
* `#course-landing` у вітрині `/design-system` -- **живими** класами, а не
  мокапом з інлайн-стилів, як решта сторінки: демонстрація ламається разом
  із компонентом і сама показує тёмну тему;
* `courses/detail.html` уже підключає новий файл, тож поточна верстка
  працює до переробки в треку D.

Тёмна тема: секції, зумисне тёмні в обох темах (hero, відгуки, лід-форма),
побудовані на `--apple-grad-dark-*` -- ці токени в тёмній темі не
перевизначаються, тому окремих `[data-theme]`-гілок не потребують. Явні
переозначення лишились тільки там, де світла картка лежить на тёмному тлі
(картка місць, форма) і потребує видимої межі.

Тест `test_hidden_attribute_css.py` оновлено: реєстр компонентів із власним
`display` тепер вказує на `course-landing.css` замість `page-courses.css`.

Зроблене у треку F:

* **ROI-калькулятор переробляти не довелося.** `roi-calc.js` уже рахує саме те,
  що дає референс -- «N процедур (≈ M тижнів)» плюс місячний дохід, із
  українською плюралізацією та i18n. Відрізнявся лише орган керування, а
  `.value` в range і number читається однаково. Тому додано тільки дзеркала
  значень (`data-roi-check-out`, `data-roi-count-out`) і стилі повзунків;
  свій `pluralUk` у скрипті лишили -- він там був до нас і працює;
* **лайтбокс виніс в окремий компонент.** `blog-lightbox.js` виявився вже
  спільним -- його підключає й сторінка тренерів, -- але стилі лежали в
  `blog.css`, через що сторінка тренерів вантажила увесь блог (типографіка
  допису, коментарі, теги) заради двадцяти рядків оверлея. Тепер
  `js/lightbox.js` + `css/lightbox.css`, класи `.blog-lightbox*` ->
  `.iprm-lightbox*`. Шаблони цих класів не знали (розмітку будує JS, у
  шаблонах лише `[data-lightbox]`), тож перейменування не зачепило жодного
  шаблону. Сторінка тренерів більше не тягне `blog.css`;
* новий `js/page-nav.js` -- активна секція через `IntersectionObserver`,
  позначається `aria-current="true"` (одне джерело правди для CSS і
  скрінрідера замість пари «клас + aria»); за кількох секцій у кадрі активною
  вважається найвища, інакше підсвітка стрибала б уперед на довгих секціях;
  активне посилання підкручується у видиму частину на вузькому екрані;
* новий `js/progress-fill.js` -- `data-progress` -> `--iprm-progress`, значення
  затискається в 0..100 (скасування реєстрацій і ручні правки місткості дають
  вихід за межі). Доріжка рендериться з `[hidden]` і показується лише після
  заповнення: без JS порожній індикатор читався б як «зайнято 0%», тобто брехав
  би, тоді як текст поруч лишається завжди;
* `sticky-cta.js` не змінювався: він лише перемикає `[hidden]`, тож
  двокнопковий варіант працює без правок.

Перейменування JS покрите наявним тестом `test_i18n/test_js_strings.py` -- він
сканує теку зі скриптами цілком, а не список файлів.

Зроблене у треках D і G:

* `courses/detail.html` перебудовано за порядком секцій із цього плану; усе
  спільне винесено в партіали, які трек E перевикористає без змін:
  `_course_page_nav`, `_course_hero`, `_course_proof`, `_course_benefits`,
  `_course_audience`, `_course_gallery`, `_course_program`, `_course_trainer`,
  `_course_reviews`, `_course_lead_form`;
* партіали читають `course.*` напряму: нові поля названі однаково в `Course`
  і `OnlineCourse`, тож обгортка не знадобилась. Різниться лише те, що
  сторінка передає явно (заголовок, CTA, дії);
* мертвий `partials/_event_trust.html` (захардкоджені 500+/30+/15+, нікуди не
  підключений) видалено -- його замінив дані-орієнтований `_course_proof`;
* таби «Детальний опис / Для кого / Що ви отримаєте» розгорнуто в самостійні
  секції, як у референсі; захардкоджений список «що ви отримаєте» замінено
  полем `benefits`;
* виділення тарифу тепер `tariff.is_featured`, а не `loop.last`: порядок
  сортування більше не вирішує за маркетинг. Додано прапорець (`badge`) і
  «+»-пункти через `description_entries`;
* ROI переведено на повзунки; ціна скрізь одна -- `min_price`, той самий
  мінімум, що в hero, ROI і sticky-CTA (раніше sticky рахував свою);
* `courses/routes.py` -- галерея в контекст; `course_request` приймає `name`,
  `messenger` (звіряється з переліком моделі, чуже значення тихо відкидається,
  щоб CHECK не завалив заявку цілком) і `consent_at`;
* `.apple-btn--full` переїхав із `page-online-course.css` в `apple-pages.css`:
  модифікатор системи кнопок жив у файлі однієї сторінки, тож на очному курсі
  мовчки не діяв;
* `.iprm-prose` отримав стилі -- класу не було в жодному CSS-файлі, детальний
  опис курсу рендерився браузерними дефолтами;
* поля лід-форми стилізовані власними правилами: глобальний `.form-input`
  діє лише під `.form-section`, в auth і в адмінці.

Три пастки, на які наступили і які варто памʼятати:

* `{% set _ = ... %}` затирає gettext-функцію `_`, і наступний `_('...')`
  падає з `TypeError: 'NoneType' object is not callable`. У проєкті для цього
  вже є конвенція `_discard` -- саме тому вона й зʼявилась;
* `flask_babel.gettext` інтерполює сам, тож `_('%(x)s') % {...}` падає з
  `KeyError`: значення передаються аргументами `_('%(x)s', x=...)`;
* Jinja-коментар `{# #}` не можна ставити всередині виразу (`{% set %}` зі
  словником) -- «unexpected char '#'».

Втрату тегів курсу з hero спіймав `test_translation_pipeline` -- саме той
тест, що перевіряє, чи доїжджають переклади на публічну сторінку.

Зроблене у треку E:

* `online/detail.html` зібрано з тих самих партіалів, що й очна сторінка --
  жоден із них не довелося правити: нові поля названі однаково в `Course` і
  `OnlineCourse`, тож обгортка не знадобилась. Сторінка виросла зі 147 рядків
  до повноцінної структури;
* прибрано все, що прив'язане до проведень (дати, місця, тарифи, графік,
  ROI, реферальне посилання, сертифікат) -- в онлайн-курсі цього немає за
  визначенням. Блок купівлі лишився на місці тарифів;
* `online/routes.py` -- галерея й відгуки в контекст. Логіку промокодів і
  чекауту не чіпали;
* картка купівлі вже була липкою; тепер її зсув враховує навігацію сторінкою
  через спільну змінну `--iprm-page-nav-h`. Змінна оголошена в
  `course-landing.css` на `.apple-page`, тож сторінки без навігації чесно
  бачать fallback 0 і нічого не зсувають дарма;
* мертві після переверстки правила `.iprm-online-detail__text` і
  `__eyebrow` прибрано: опис тепер `.iprm-prose`, надзаголовок -- чип hero;
* у блоці «інші онлайн-курси» змінну циклу перейменовано з `course` на
  `related_course` (+`{% with %}`): раніше цикл перезаписував `course`, і на
  новій сторінці це зіпсувало б секції нижче.

Перевірено рендером двох станів: курс із повним контентом (усі 13 маркерів
секцій на місці) і курс без контенту (усі необов'язкові секції приховані,
сторінка ціла).

Зроблене у треку B:

* **формати введення спільні для обох форм** -- інакше редактор мусив би
  памʼятати, що «в онлайні інакше». Два парсери в `course_service`:
  `pairs_text_to_list` («значення | підпис» -- цифри довіри й цифри тренера)
  і `blocks_text_to_list` (блок = порожній рядок, перший рядок -- заголовок).
  Другий узагальнено з наявного FAQ-парсера, і `faq_text_to_list` тепер
  просто його викликає -- одна механіка замість двох схожих;
* роздільник пар -- саме `|`: тире й дефіси трапляються всередині самих
  значень («13-15 років»), а вертикальна риска -- ні;
* **галерея не має власної таблиці.** Редактор оперує лише порядком і
  підписами: вантажить файл у спільний `/admin/upload/media`, а в приховане
  поле кладе `[{media_id, caption, thumb}]`. `save_gallery` розставляє
  `entity_type`/`usage_type='gallery'`/`sort_order`/`caption` на рядках
  реєстру. Прибране з галереї **не видаляється** -- лише відвʼязується:
  файл міг використовуватись деінде, а видалення з реєстру -- окрема
  свідома дія в медіа-бібліотеці;
* новий `js/admin-course-gallery.js` + `css/admin-gallery.css`: мультизавантаження,
  drag-порядок, підпис на кожне фото. Файли вантажаться послідовно, а не
  `Promise.all`: пачка великих фото паралельно впирається в ліміти сервера,
  а порядок додавання стає випадковим;
* `save_program_blocks_for_course` узагальнено до `_save_program_blocks(owner,
  data, relation)` -- імʼя звʼязку передається явно, бо CHECK у БД вимагає
  рівно одного власника й вгадувати його не можна. Онлайн-курс отримав
  `save_program_blocks_for_online_course`;
* **`admin-event-edit.js` перевикористано без правок** -- редактор блоків
  програми виявився повністю загальним (працює з `#program-blocks` і
  `block_N_*`), тож онлайн-формі вистачило тієї самої розмітки;
* форми: курс -- секції «Продажна сторінка» і «Галерея з практики» з
  i18n-панелями на кожному перекладному полі; онлайн-курс -- те саме плюс
  тренер, аудиторія, FAQ і блоки програми; тариф -- `badge` і «Виділити на
  сторінці курсу»; тренер -- `highlights_text` і підказка, що перші чотири
  навички без двокрапки стають тегами.

Перевірено наскрізними POST-ами в обидві форми: усі поля зберігаються,
блок програми онлайн-курсу лягає з `course_id=None, online_course_id=<id>`
(CHECK задоволений), галерея зберігає порядок і підписи, прибраний файл
лишається в реєстрі з `entity_type=None`, зіпсований JSON ігнорується
з попередженням у лог.

Зроблене у треку H:

* каталоги ru/en заповнено повністю: **0 неперекладених, 0 fuzzy** (було 26 і
  36 на мову). Серед них -- і рядки паралельної роботи над промокодами й
  рахунками: fuzzy-записи pybabel НЕ компілює, тож вони показувалися б
  українською в усіх локалях;
* великий дифф каталогів -- переформатування переносів рядків самою
  `pybabel update`, а не мій артефакт: повторний запуск канонічної команди
  дає той самий результат, тобто ця churn прийшла б із наступним оновленням
  каталогів у будь-якому разі;
* `FAQPage` виніс у партіал `_course_faq_jsonld.html` -- окремим блоком, бо
  schema.org не дозволяє вкладати `FAQPage` у `Course`, а Google читає кілька
  незалежних JSON-LD на сторінці. Підключений на обох сторінках; питання
  беруться через `.t()`, тобто мовою сторінки;
* `image` у схемі курсу став масивом «обкладинка + фото галереї»;
* 26 нових тестів у трьох файлах: `test_course_landing.py` (секції на обох
  сторінках, порожній курс, якорі навігації, теги в hero, FAQPage),
  `test_course_landing_content.py` (парсери пар і блоків, синхронізація
  галереї, зіпсований JSON), `test_course_request_fields.py` (нові поля
  заявки, згода, невідомий канал звʼязку);
* README і `docs/routes.md` оновлено.

Перевірено рендером у трьох локалях: нові рядки доїжджають на uk/ru/en, і
українська не протікає на /ru.

**Пастка для перевірок вручну:** Flask-Babel кешує обрану локаль на `g`. У
скрипті з одним app-контекстом усі три запити віддадуть мову першого -- саме
для цього в `tests/test_i18n/conftest.py` є `get_localized`.

## Рамки (узгоджено 17.08.2026)

| Питання | Рішення |
|---|---|
| Які сторінки | **Обидві**: `courses/detail.html` (очні) і `online/detail.html` (онлайн) |
| Дизайн-система | **Лише нові компоненти.** Токени сайту (`--iprm-accent #7055a4`, `--apple-orange #e8913a`) НЕ чіпаємо. Від референсу беремо композицію, ритм, щільність інформації, типографічні пропорції |
| Дані нових модулів | **БД + адмінка.** Нові поля/зв'язки, міграції, форми редагування, i18n-панелі |
| Викочування | **Одразу на всі курси.** Без перемикача макета. Порожні модулі приховуються умовно |

Наслідок рішення про дизайн-систему: фіолетовий референсу `#7437c9` і золотий
`#ffad16` НЕ переносяться. Усі нові компоненти малюються в наявній палітрі й
зобов'язані працювати в тёмній темі (`[data-theme="dark"]`) -- референс
світлий-only, тому кожен градієнт/тінь із нього потребує тёмного відповідника.

## Матриця модулів

| Модуль референсу | Стан у нас | Дія |
|---|---|---|
| Hero: чипи (дата·місто, формат, група), H1 з акцентним словом, лід, 2 CTA, 4 KPI, фото + плаваюча картка «6 вільних місць» із прогрес-баром | Є hero: заголовок, підзаголовок, `roi_hint`, теги, 2 CTA. Немає фото, KPI-рядка, чипів, картки місць | **Переверстати.** Дані є всі: `format_inst`, `seats_left_map`, `max_participants`, `hero_src`, `city`, `cpd_points` |
| Proof-смуга: 4 цифри під hero | Немає | **Новий.** Поле `Course.proof_stats` |
| «Що зміниться у практиці» -- 6 карток із номерами | Немає (частково таб «Що ви отримаєте» з 4 захардкодженими пунктами) | **Новий.** Поле `Course.benefits` |
| ROI-калькулятор | Є (`#roi`, `roi-calc.js`): числові поля, результат «дохід/окупність» | **Переверстати + змінити механіку** на слайдери й формулювання «N процедур ≈ M тижнів» |
| «Для кого» + картка зворотного дзвінка | Є як таб із чипами (`target_audience`) | **Розгорнути з табів у секцію** з 2 карток, друга -- CTA на лід-форму |
| Галерея 6 фото з підписами | Немає | **Новий.** `MediaFile.usage_type='gallery'` (вже в `USAGE_TYPES`) + `MediaFile.caption` |
| Програма 2 блоки + акцентна плашка «ви робите руками» | Програма є (`ProgramBlock`). Плашки немає | **Переверстати + додати** `practice_note_title/text` |
| Тренер: фото, теги, 3 стати | Є фото + роль + біо | **Переверстати + додати** `Trainer.highlights`; теги -- з наявного `Trainer.skills` |
| Відгуки в темній оболонці + Instagram-CTA | Є `reviews-grid` + `_review_card.html` | **Переверстати** (темна оболонка -- новий варіант секції) |
| Тарифи: «best» із прапорцем, «+»-пункти | Є `iprm-format-card--featured` (за `loop.last`) | **Переверстати + додати** `InstanceTariff.badge`, `is_featured`; «+»-пункти -- конвенцією у тексті опису |
| FAQ (`<details>`) | Є, така сама механіка | **Переверстати** (тонше, «+»→«×» замість шеврона) |
| Лід-форма: ім'я, телефон, месенджер, email, згода | Є форма запиту: email, телефон, повідомлення | **Переверстати + розширити** `CourseRequest` |
| Sticky CTA | Є (`iprm-sticky-cta`, `sticky-cta.js`) | **Переверстати** під двокнопковий варіант референсу |
| Якірна навігація в шапці | Немає (глобальна шапка сайту) | **Новий**: sticky sub-nav сторінки курсу |

Модулі, яких у референсі немає, а в нас є -- **лишаємо і редизайнимо** під новий
ритм: найближчі дати (`#schedule`), історія проведень, умови сертифіката
(`_event_certificate.html`), рекомендовані курси, реферальне посилання,
фінальний CTA, банер запрошення реферала, Meta Pixel `ViewContent`.

## Порядок секцій нової сторінки

Очний курс (`courses/detail.html`). Одразу під глобальною шапкою -- sticky
sub-nav (`iprm-page-nav`) з якорями на секції 3, 7, 9, 11, 14 і індикатором
вільних місць; далі:

1. Hero (чипи, H1, лід, 2 CTA, 4 KPI, фото + картка місць)
2. Proof-смуга
3. Що зміниться у практиці (6 карток)
4. Про курс -- детальний опис (з таба в повноцінну секцію)
5. Для кого + картка «є питання»
6. Галерея
7. Програма + practice-note
8. Тренер
9. Формат участі / тарифи
10. Окупність (ROI)
11. Найближчі дати
12. Історія проведень
13. Умови сертифіката
14. Відгуки (темна оболонка)
15. FAQ
16. Рекомендовані курси
17. Реферальне посилання (для залогінених)
18. Лід-форма (темна секція)
19. Фінальний CTA + sticky CTA

Чергування фонів (`apple-section-gray`) перераховується під цю послідовність --
поточний коментар у шаблоні (рядки 151-158) підлягає заміні.

Онлайн-курс (`online/detail.html`) -- та сама сітка мінус блоки, прив'язані до
проведень: 1, 2, 3, 4, 5, 6, 7, 8, 14, 15, 16, 18, 19 + блок купівлі замість
тарифів. Тобто це не косметична правка, а перебудова сторінки зі 147 рядків до
структури, порівнянної з очною.

## Треки для окремих агентів

Порядок і залежності:

```
A (дані) ──┬──> B (адмінка)
           ├──> D (шаблон очного) ──┐
           └──> E (шаблон онлайн) ──┤
C (CSS) ───────────────────────────┼──> H (i18n, SEO, тести)
F (JS) ────────────────────────────┘
G (лід-форма) -- паралельно, залежить від A
```

C і F можна починати одночасно з A. D і E не стартують, поки A і C не злиті.

---

### Трек A -- дані: моделі та міграції

Файли: `app/models/course.py`, `app/models/online_course.py`,
`app/models/trainer.py`, `app/models/instance_tariff.py`,
`app/models/media_file.py`, `app/models/course_request.py`,
`migrations/versions/*`, `docs/models.md`.

Нові поля:

* `Course` (+ у `__translatable__`): `benefits` JSON `[{title, text}]`,
  `proof_stats` JSON `[{value, label}]`, `practice_note_title` String(200),
  `practice_note_text` Text, `gallery_intro` String(500).
* `OnlineCourse` (+ у `__translatable__`): ті самі п'ять + `target_audience`
  JSON, `faq` JSON, `final_cta_text` String(300), `trainer_id` FK →
  `trainers.id` ON DELETE SET NULL.
* `Trainer`: `highlights` JSON `[{value, label}]` (3 елементи в референсі).
  Теги -- наявне `skills`, нового поля не треба.
* `InstanceTariff`: `badge` String(60) (+ до `__translatable__`),
  `is_featured` Boolean default False. Прибирає хардкод `loop.last` у шаблоні.
* `MediaFile`: `caption` String(255). **Свідомо не транслейтабельне** -- як і
  наявне `alt_text`. Підписи галереї показуються однією мовою (uk) в усіх
  локалях. Якщо колись знадобиться переклад, `MediaFile` доведеться заводити
  під `TranslatableMixin` -- це зачепить блог і тренерів, тож зараз не робимо.
* `CourseRequest`: `name` String(120), `messenger` String(20),
  `consent_at` DateTime(timezone=True).

**Поліморфна прив'язка програми і відгуків** (рішення від 17.08.2026 -- заради
DRY, щоб редактор блоків в адмінці й партіал шаблону були спільні):

* `program_blocks`: `course_id` стає nullable, додається `online_course_id`
  BigInteger FK → `online_courses.id` ON DELETE CASCADE, nullable, index. Плюс
  `CheckConstraint('num_nonnulls(course_id, online_course_id) = 1',
  name='ck_program_blocks_owner')`. Відношення: `ProgramBlock.online_course` +
  `OnlineCourse.program_blocks` (back_populates, `order_by=sort_order`).
* `reviews`: `course_id` **уже** nullable («опційна прив'язка до курсу»), тому
  тут лише додається `online_course_id` nullable FK ON DELETE SET NULL. CHECK не
  потрібен -- відгук може не належати жодному курсу, це наявна поведінка
  (`Review.published()`).

Обережно з CHECK: у проєкті вже була пастка, коли обмеження на рівні БД
розійшлося з тим, що пише застосунок (див. пам'ять `project_email_system`).
Перед додаванням CHECK перевірити, що жоден наявний рядок `program_blocks` його
не порушує, і що адмінка ніколи не створює блок без власника.

Міграції: `course_landing_20260817` (усі нові колонки) і
`program_blocks_polymorphic_20260817` (nullable + FK + CHECK) -- окремими
ревізіями, щоб ризиковану можна було відкотити самостійно. **Локальний `.env` і
прод ділять одну БД** -- жодного `flask db upgrade` локально без явної команди
(див. пам'ять `feedback_shared_db_hazard`).

Приймання: `flask db upgrade` / `downgrade` проходять на порожній локальній БД;
`docs/models.md` оновлено; наявні тести зелені.

---

### Трек B -- адмінка

Файли: `app/admin/routes_courses.py`, `app/admin/routes_online_courses.py`,
`app/admin/routes_trainers.py`, `app/admin/routes_instance_tariffs.py`,
`app/admin/forms*`, `app/templates/admin/course_edit.html`,
`app/templates/admin/online_course_edit.html`, `admin/trainer_edit.html`,
`app/static/js/admin-course-gallery.js` (новий),
`app/static/css/admin-*.css`.

Роботи:

1. Нові секції у формі курсу: «Переваги» (6 пар заголовок/текст), «Цифри
   довіри» (4 пари), «Акцент практики». Кожне транслейтабельне поле обов'язково
   отримує `i18n.panes` / `i18n.json_panes` -- інакше поле випадає з uk/ru/en.
2. Галерея: мультизавантаження, drag-sort, підпис до кожного файла.
   Перевикористати `admin-dropzone.js` + `admin-media-picker.js`; логіку
   прив'язки скопіювати з `_attach_media` у `app/admin/routes_blog.py:138`
   (виставляє `entity_type`/`entity_id`/`usage_type`/`sort_order` і перейменовує
   файли у читабельну схему `{slug}-gallery-N`).
3. Форма онлайн-курсу: ті самі секції + вибір тренера + редактор програми + FAQ.
   Редактор блоків програми перевикористати з `course_edit.html` (розмітка
   `block_{i}_heading` / `block_{i}_items` + `i18n.panes` з префіксом
   `trblk__{id}`) -- після поліморфної міграції з треку A він працює для обох
   типів курсів без розгалуження.
4. Форма тарифу проведення: `badge`, `is_featured`; підказка про конвенцію
   «+»-пунктів у описі.
5. Форма тренера: `highlights` (3 пари значення/підпис).
6. **Заливка галереї «Плазмогелю».** 6 файлів `course-ruban-01..06.png` із
   `k:\Downloads\IPRM-plazmogel-landing-ready\plazmogel-landing-assets\`
   завантажити через звичайний аплоад адмінки (`media_service` сам зробить WebP
   і `responsive_variants`), проставити `entity_type='course'`,
   `entity_id=<id Плазмогелю>`, `usage_type='gallery'`, `sort_order` 1..6 і
   підписи з референсу (рядки 358-363: «Навчання в обладнаному клінічному
   кабінеті», «Покрокове приготування плазмогелю», «Практика на моделі під
   наглядом тренера», «Робота з центрифугою та термостатом», «Контроль техніки
   та персональні підказки», «Постановка руки на реальній практиці»).
   **Це пише в продакшн-БД і продакшн-медіа** -- локальний `.env` дивиться в ту
   саму базу. Робити останнім кроком треку, після того як міграція
   `course_landing_20260817` уже на проді, і тільки за явної команди. Файли
   `hero-640x500.png` і `trainer-serkutan.jpg` -- окремо, як hero курсу й фото
   тренера, якщо поточні гірші.

Приймання: усі нові поля зберігаються й читаються в трьох мовах; галерея
переживає редагування без втрати порядку і підписів; існуючі тести адмінки
зелені.

---

### Трек C -- дизайн-система: CSS-компоненти

Файли: **новий** `app/static/css/course-landing.css` (спільний для очної й
онлайн-сторінок), `app/static/css/design-system.css`,
`app/templates/design_system/_content.html`.

Чому окремий файл: `page-courses.css` уже 2055 рядків, а компоненти потрібні
двом сторінкам. Це відповідає External Assets Pattern і полегшить майбутній
`css-split`.

Нові компоненти (BEM, префікс `iprm-`):

* `iprm-hero--landing` -- сітка 1.08fr/0.92fr, чипи, KPI-рядок, фото, картка
  місць із прогрес-баром
* `iprm-proof` -- смуга з 4 цифр
* `iprm-benefit-grid` / `iprm-benefit-card` -- 6 карток із номерною плашкою
* `iprm-audience-grid` -- 2 картки з кольоровою верхньою межею
* `iprm-gallery` -- сітка 3×2, `aspect-ratio: 4/5`, підпис-оверлей, hover-zoom
* `iprm-practice-note` -- акцентна плашка
* `iprm-trainer-block--rich` -- фото 3/4, теги, 3 стати
* `iprm-reviews-shell` -- темна оболонка секції відгуків
* `iprm-format-card__flag` -- прапорець тарифу + модифікатор `--plus` для пунктів
* `iprm-faq` -- переверстка на «+»/«×»
* `iprm-lead-section` -- темна секція з формою
* `iprm-page-nav` -- sticky якірна навігація сторінки з індикатором вільних
  місць (`iprm-page-nav__seats` -- крапка + текст, як `.nav-note` у референсі).
  **Критичний компонент, не декоративний:** сторінка має 19 секцій, і без
  навігації нею неможливо користуватися. Позиціонується під глобальною шапкою
  (`top: var(--iprm-header-h)`), не конфліктує з нею по z-index
* `iprm-sticky-cta--dual` -- дві кнопки

Обов'язкове:

* тёмні відповідники для КОЖНОЇ поверхні (`[data-theme="dark"]` -- єдине
  авторитетне місце, без `@media prefers-color-scheme`);
* не використовувати `--iprm-white` для тексту поверх акцентного тла: у тёмній
  темі це темна поверхня. Тільки `--iprm-on-accent` (див. коментар у
  `common.css:10`);
* брейкпойнти узгодити з наявними в `page-courses.css`, не вводити нові;
* `prefers-reduced-motion` для hover-zoom галереї;
* усі 12 компонентів додати у вітрину `design_system/_content.html`.

Приймання: сторінка `/design-system` показує кожен компонент у світлій і тёмній
темах; жодного inline-стилю; жодного нового кольору поза токенами.

---

### Трек D -- шаблон очного курсу

Файли: `app/templates/courses/detail.html`, нові партіали в
`app/templates/partials/` (`_course_hero.html`, `_course_gallery.html`,
`_course_benefits.html`, `_course_proof.html`, `_course_trainer_rich.html`,
`_lead_form.html`), `app/courses/routes.py` (галерея в контекст).

Роботи: перебудувати сторінку за порядком секцій вище; кожен новий блок
загорнути в `{% if %}` -- курси без контенту не повинні показувати порожні
секції; винести повторювані блоки в партіали, щоб трек E їх перевикористав;
перерахувати чергування `apple-section-gray`; оновити коментар про порядок
блоків; зберегти всі наявні інтеграції (Meta Pixel, реферальний банер, reCAPTCHA,
`data-ga-event`).

Запит галереї в роуті: `MediaFile.query.filter_by(entity_type='course',
entity_id=course.id, usage_type='gallery', is_active=True).order_by(sort_order)`
-- одним запитом разом із рештою даних курсу, без N+1.

Приймання: сторінка курсу «Плазмогель» візуально відповідає референсу за
композицією; курс із порожнім новим контентом рендериться без порожнин і
помилок; Lighthouse не гірший за поточний.

---

### Трек E -- шаблон онлайн-курсу

Файли: `app/templates/online/detail.html`, `app/online/routes.py`,
`app/static/css/page-online-course.css`.

Роботи: перебудувати сторінку зі 147 рядків на структуру з партіалів треку D;
підключити `course-landing.css`; блок купівлі (`iprm-online-buy`) переверстати
під картку тарифу; врахувати три стани CTA (вже куплено / доступна купівля /
купівля недоступна) і промокоди -- вони зараз у роботі, гілка `main` має
незакомічені зміни в `app/online/routes.py` і `checkout.html`.

**Увага на конфлікт:** `app/online/routes.py`, `app/models/online_enrollment.py`,
`app/models/promo_code.py`, `app/services/promo_service.py`,
`app/templates/online/checkout.html` і `page-online-course.css` зараз змінені й
не закомічені. Трек E стартує тільки після того, як ця робота закомічена --
інакше конфлікти гарантовані.

Приймання: онлайн-сторінка має ті самі компоненти, що й очна, у тій самій
палітрі; купівля й промокод працюють як раніше.

---

### Трек F -- JS-інтерактив

Файли: `app/static/js/roi-calc.js`, `app/static/js/course-gallery.js` (новий,
або перевикористати `blog-lightbox.js`), `app/static/js/page-nav.js` (новий),
`app/static/js/sticky-cta.js`, `app/static/js/faq-accordion.js`.

Роботи:

1. ROI: перевести на `<input type="range">` з живим `<output>`; змінити
   результат на «N процедур ≈ M тижнів» + місячний оборот; плюралізацію брати з
   наявного `i18n.js` / `plural`-фільтра, а не з локального хелпера як у
   референсі. Ціна курсу так само з `data-roi-price`.
2. Лайтбокс галереї: перевірити, чи `blog-lightbox.js` покриває сценарій
   (`[data-lightbox]`) -- якщо так, підключити його, не плодити другий.
3. Sticky якірна навігація (`page-nav.js`): підсвічування активної секції через
   `IntersectionObserver`, `scroll-padding-top` під висоту глобальної шапки
   (`--iprm-header-h`) плюс висоту самого sub-nav. Посилання рендеряться в
   шаблоні лише для секцій, які реально є на сторінці, -- порожній курс не
   повинен мати битих якорів. На вузьких екранах -- горизонтальний скрол
   без обрізання. Це блокер треків D/E (див. рішення 3).
4. Прогрес-бар вільних місць: ширина -- це дані, а `style` заборонено
   (No Inline Policy), тож шаблон віддає число в `data-progress`, а JS кладе
   його в CSS-змінну `--iprm-progress`. Варіант «CSS-змінна в атрибуті» не
   годиться: це той самий інлайн-стиль.
5. Sticky CTA: варіант із двома кнопками; на вузьких екранах ховається
   другорядна.

Приймання: усі скрипти `defer`, без inline; працюють без JS (Progressive
Enhancement) -- ROI показує статичний текст, галерея відкриває картинку за
прямим посиланням.

---

### Трек G -- лід-форма і заявки

Файли: `app/courses/routes.py` (`course_request`), `app/models/course_request.py`,
`app/admin/routes_course_requests.py`, `app/templates/admin/course_requests.html`,
`app/templates/admin/course_request_edit.html`, шаблони листів у
`app/templates/emails/`, `app/services/notification*`.

Роботи: додати поля ім'я / месенджер / згода до форми і до збереження; вивести
їх у списку та картці заявки в адмінці; додати у лист-сповіщення
адміністратору. Зберегти honeypot, reCAPTCHA, `data-single-submit`,
`data-ga-event="generate_lead"`. Referrer/UTM referrer -- якщо вже пишуться, не
ламати.

Форма референсу шле `mailto:` -- це ми **не** переносимо: у нас заявка йде в БД
і в сповіщення, це строго краще.

Приймання: заявка з новими полями зберігається, показується в адмінці й
приходить у листі; згода фіксується часом.

---

### Трек H -- i18n, SEO, продуктивність, тести

Роботи:

1. `pybabel extract/update/compile`, переклад усіх нових рядків на ru/en.
   Перевірити, що нові JSON-поля мають `json_panes` і реально віддають
   переклад через `.t()`.
2. JSON-LD: додати `image` з галереї, `aggregateRating` якщо є відгуки,
   `FAQPage` для блоку FAQ (зараз FAQ у розмітці є, а в schema немає).
3. Продуктивність: hero-фото -- `fetchpriority="high"` + preload (уже є);
   галерея -- `loading="lazy"` + `width`/`height` + WebP-варіанти з
   `responsive_variants`; стежити за CLS від нових секцій.
4. Тести: рендер сторінки курсу з повним і з порожнім контентом; збереження
   нових полів в адмінці; прив'язка галереї; заявка з новими полями.
5. Оновити `README.md`, `docs/routes.md`, `docs/models.md`,
   `docs/apple-design-patterns.md`.

Приймання: `pytest` зелений; `/design-system` і сторінки курсів без помилок
консолі; каталоги перекладів скомпільовані.

## Ухвалені рішення (17.08.2026)

1. **Програма і відгуки -- поліморфні** (`online_course_id` поряд із
   `course_id`), заради DRY. Деталі й застереження -- у треку A.
2. **Підписи галереї -- `MediaFile.caption` без перекладу**, як `alt_text`.
   Одна мова в усіх локалях.
3. **Довжина сторінки -- лишаємо всі 19 секцій.** Нічого не ріжемо і не
   ховаємо під спойлери. Наслідок: sticky якірна навігація (`iprm-page-nav`) з
   декоративної стає обов'язковою -- без неї сторінку не пройти. Тому вона
   переходить у розряд блокерів треків D/E, а не «приємного доповнення».
4. **6 фото «Плазмогелю» заливаємо в медіа-реєстр** у межах роботи -- див.
   окремий підпункт у треку B. Це єдина операція плану, що одразу пише в
   продакшн-дані.
5. **Instagram -- із `SiteSettings`.** Поле `instagram_url` уже існує
   (`app/models/site_settings.py:73`) і його дефолт -- саме
   `https://www.instagram.com/Plasmotherapy/`. Нового поля не треба; у шаблоні
   `{% if site_settings.instagram_url %}`, як у
   [footer.html:17](app/templates/partials/footer.html#L17).
6. **«Залишилось N місць» -- у sticky sub-nav**, як у topbar референсу.
   Дублюється в hero-картці (там із прогрес-баром), у sub-nav -- компактний
   варіант із крапкою-індикатором. Показується тільки коли є відкрите
   проведення з відомим `seats_left`.

## Ризики

* **Тёмна тема.** Референс світлий-only. Кожна темна секція (hero, відгуки,
  лід-форма) у тёмній темі має не зливатися з фоном сторінки. Це найбільший
  ризик візуальних регресій.
* **Курси без нового контенту.** Рішення «одразу на всі курси» означає, що
  13 наявних курсів покажуть сторінку без proof-цифр, переваг і галереї, доки
  контент не заповнять. Умовний рендер обов'язковий, і сторінка має лишатися
  цілісною з половиною порожніх модулів.
* **Незакомічена робота по онлайн-курсах і промокодах** -- див. трек E.
* **Спільна БД з продом** -- міграції не запускати локально. Заливка галереї
  (трек B, п. 6) з тієї ж причини одразу потрапляє на прод: робити останнім
  кроком і за явної команди.
* **19 секцій -- довга сторінка.** Свідоме рішення, але ціна: sub-nav стає
  критичним шляхом, а не оздобленням, і кожна секція має «заробляти» місце.
  Якщо CTA почне губитися -- першими кандидатами на згортання лишаються
  «історія проведень» і «реферальне посилання».
* **CHECK на `program_blocks`** -- обмеження БД може розійтися з тим, що пише
  адмінка. Перед міграцією перевірити наявні рядки й усі місця створення блоку.
* **`page-courses.css` 2055 рядків.** Не роздувати далі; усе нове -- у
  `course-landing.css`.
