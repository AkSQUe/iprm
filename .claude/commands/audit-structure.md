Ти - експерт з архітектури Flask-додатків та найкращих практик веб-розробки.

## Мета

Провести аудит файлової структури проекту IPRM на відповідність золотим правилам Flask-розробки та розробити план оптимізації.

$ARGUMENTS

Якщо аргументи не передано - провести повний аудит всієї структури.

## Контекст проекту

- Тип: Flask 3.0+ Application Factory + Blueprint архітектура
- ORM: SQLAlchemy, міграції через Flask-Migrate/Alembic
- Frontend: Jinja2 + CSS + Tailwind CSS (гібридна архітектура)
- БД: SQLite (dev), PostgreSQL (prod)
- Деплой: GitHub Actions + rsync на VPS
- Документація архітектури: `docs/architecture.md`
- Принципи проекту описані в CLAUDE.md

## Крок 1: Збір даних

Вивчи повну файлову структуру проекту:
- Кореневий рівень: всі файли та каталоги
- `app/` та всі вкладені каталоги рекурсивно
- `migrations/`, `docs/`, `.github/`, `instance/`, `keys/`
- Прочитай ключові файли: `app/__init__.py`, `config.py`, `run.py`, `requirements.txt`
- Прочитай `__init__.py` в кожному blueprint-каталозі
- Прочитай `docs/architecture.md` для порівняння документованої vs реальної структури

## Крок 2: Аудит за категоріями

Перевір кожну категорію і оціни стан: OK / WARNING / ISSUE.

### 2.1 Application Factory та точка входу
- `run.py` - чи правильно викликає `create_app()`?
- `app/__init__.py` - чи використовує Application Factory pattern?
- Чи винесені розширення в `extensions.py`?
- Чи немає circular imports?

### 2.2 Blueprint-архітектура
- Кожен blueprint: чи має `__init__.py` з реєстрацією Blueprint?
- Чи використовує `routes.py` (не `views.py`)?
- Чи є зайві файли всередині blueprint-каталогів?
- Чи дотримано принцип Single Responsibility?
- Чи немає "товстих" routes (бізнес-логіка має бути в services/models)?

### 2.3 Моделі та база даних
- Чи розділені моделі на окремі файли (не один великий `models.py`)?
- Чи є `__init__.py` з правильними імпортами моделей?
- Чи використовується TimestampMixin?
- Чи правильна структура `migrations/`?
- Чи немає міграцій, що конфліктують?

### 2.4 Статичні файли
- CSS: чи відповідає конвенції іменування (common, page-*, blueprint-name)?
- JS: чи модульні скрипти, чи немає inline JS?
- SVG/зображення: чи організовані по підкаталогах?
- Чи немає невикористаних файлів?

### 2.5 Шаблони
- Чи є `base.html` з базовим layout?
- Чи кожен blueprint має свій каталог шаблонів?
- Чи використовуються `partials/` для header/footer/reusable компонентів?
- Чи немає дублювання HTML між шаблонами?
- Чи відповідають імена шаблонів маршрутам?

### 2.6 Конфігурація та безпека
- `config.py` - чи є Dev/Prod/Test конфігурації?
- `.env` та `.env.example` - чи є приклад для нових розробників?
- `.gitignore` - чи виключені: `.env`, `instance/`, `__pycache__/`, `*.pyc`?
- Чи немає секретів у tracked файлах?
- `keys/` каталог - чи в `.gitignore`?

### 2.7 CI/CD та DevOps
- `.github/workflows/` - чи є pipeline?
- Чи є `requirements.txt` або `pyproject.toml`?
- Чи є `Procfile` або інший файл для production?

### 2.8 Документація
- Чи є `README.md` з описом проекту?
- Чи є `docs/` каталог з архітектурною документацією?
- Чи відповідає документована структура реальній?

### 2.9 Відповідність принципам
- **DRY**: чи немає дублювання коду між blueprints?
- **KISS**: чи немає overengineering?
- **SOLID / SRP**: чи кожен файл має одну відповідальність?
- **Separation of Concerns**: чи розділені шари (routes/models/templates/static)?
- **No Inline Policy**: чи немає inline CSS/JS в шаблонах?
- **Progressive Enhancement**: чи працює без JS?

### 2.10 Відсутні елементи (Flask best practices)
Перевір, чи є потреба в:
- `app/services/` - шар бізнес-логіки (якщо routes занадто товсті)
- `app/utils/` або `app/helpers/` - спільні утиліти
- `tests/` каталог з тестами
- `app/static/images/` або `app/static/uploads/` (якщо є завантаження)
- `app/forms/` - окремий каталог для форм (якщо форми складні)
- `app/cli.py` - CLI команди для management tasks
- `wsgi.py` - WSGI entry point для production

## Крок 3: Звіт

Сформуй звіт у такому форматі:

### Загальна оцінка
Коротка характеристика стану проекту (1-2 речення).

### Результати аудиту

Для кожної категорії:

**[Категорія]** - [OK / WARNING / ISSUE]
- Що добре
- Що потребує уваги (якщо є)
- Конкретна рекомендація (якщо є)

### Зведена таблиця

| Категорія | Стан | Пріоритет |
|-----------|------|-----------|
| ... | OK/WARNING/ISSUE | High/Medium/Low |

## Крок 4: План оптимізації

Якщо знайдено WARNING або ISSUE, створи план:

### Пріоритет HIGH (зробити першим)
- Проблема -> Рішення -> Файли, що потребують змін

### Пріоритет MEDIUM (зробити після high)
- Проблема -> Рішення -> Файли, що потребують змін

### Пріоритет LOW (бажано, але не критично)
- Проблема -> Рішення -> Файли, що потребують змін

## Обмеження

- НЕ вноси жодних змін до коду - тільки аналіз та звіт
- НЕ пропонуй зміни, що суперечать CLAUDE.md
- НЕ пропонуй додавання бібліотек/фреймворків без чіткого обґрунтування
- Використовуй українську мову для звіту
- Будь конкретним: замість "покращити структуру" пиши "перемістити X до Y"
