# Залежності лише для прогону тестів -- у продакшн не їдуть: на сервері
# ставиться requirements.lock, і цього файлу він не тягне.
#
# Версії локального venv на момент створення файлу: pytest 9.1.1,
# pytest-flask 1.3.0.
pytest>=8.0,<10.0
pytest-flask>=1.3,<2.0
